--- a/src/dvd_input.c
+++ b/src/dvd_input.c
@@ -425,6 +425,13 @@
  */
 int dvdinput_setup(void *priv, dvd_logger_cb *logcb, dvd_type_t dvda_flag)
 {
+#ifdef MYNDA_DISABLE_LIBDVDCSS
+  (void)dvda_flag;
+  DVDReadLog(priv, logcb, DVD_LOGGER_LEVEL_WARN,
+             "Encrypted DVD support disabled in this Mynda build.");
+  dvdinput_setup_builtin(priv, logcb);
+  return 0;
+#else
   void *dvdcss_library = NULL;
 
 #ifdef HAVE_DVDCSS_DVDCPXM_H
@@ -565,6 +573,7 @@
     dvdinput_setup_builtin(priv, logcb);
     return 0;
   }
+#endif /* MYNDA_DISABLE_LIBDVDCSS */
 }
 
 /**
